def dummy_func(x: Int): Unit {
    // LOADREF X
    // LOADI 0
    // GREATER_THAN
    // JMP_IF_FALSE 7
    // LOADREF x
    // CALL PRINT
    // LOADREF x
    // LOADI 1
    // SUB
    // SET_LOCAL x
    // JMP -7
    // RETURN
    while (x > 0) {
        print(x)
        x = x - 1
    }
}

def main(): Unit {
    val y = 5
    dummy_func(y)
}
